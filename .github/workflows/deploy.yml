name: Deploy

on:
  # Stage: auto-deploy on every push to master
  push:
    branches: [master]

  # Stage or prod: manual trigger
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - stage
          - prod

# ── Stage ─────────────────────────────────────────────────────────────────────
# Runs on:
#   - push to master (automatic)
#   - workflow_dispatch with environment=stage (manual, any branch)

jobs:
  deploy-stage:
    name: Deploy → stage
    if: github.event_name == 'push' || inputs.environment == 'stage'
    runs-on: ubuntu-latest
    environment: stage
    steps:
      - uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Function to stage
        run: |
          supabase functions deploy api \
            --project-ref ${{ secrets.SUPABASE_PROJECT_REF }} \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apxeer-web/package-lock.json

      - name: Install web dependencies
        run: npm ci
        working-directory: apxeer-web

      - name: Build web (stage)
        run: npm run build
        working-directory: apxeer-web
        env:
          VITE_API_URL: https://${{ secrets.SUPABASE_PROJECT_REF }}.supabase.co/functions/v1
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Deploy web to Cloudflare (stage)
        run: npx wrangler deploy
        working-directory: apxeer-web
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

# ── Production ────────────────────────────────────────────────────────────────
# Runs on:
#   - workflow_dispatch with environment=prod AND on the master branch only

  deploy-prod:
    name: Deploy → prod
    if: inputs.environment == 'prod' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Function to prod
        run: |
          supabase functions deploy api \
            --project-ref ${{ secrets.SUPABASE_PROJECT_REF }} \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apxeer-web/package-lock.json

      - name: Install web dependencies
        run: npm ci
        working-directory: apxeer-web

      - name: Build web (prod)
        run: npm run build
        working-directory: apxeer-web
        env:
          VITE_API_URL: https://${{ secrets.SUPABASE_PROJECT_REF }}.supabase.co/functions/v1
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Deploy web to Cloudflare (prod)
        run: npx wrangler deploy --name apxeer-web
        working-directory: apxeer-web
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
